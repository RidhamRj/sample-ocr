import type { CanonicalDocument } from "../types/canonical";
export default function DiagnosticsPanel({ document }: { document: CanonicalDocument }) { return <div className="diagnostics-grid"><section className="diagnostic-card"><h3>Unassigned OCR</h3><p>{document.unassignedOcr.length} OCR boxes were not forced into a cell.</p><div className="ocr-list">{document.unassignedOcr.slice(0, 50).map((item) => <article key={item.id}><strong>{Math.round(item.confidence * 100)}%</strong><span>{item.text}</span></article>)}{!document.unassignedOcr.length && <p className="muted">All OCR boxes were assigned.</p>}</div></section><section className="diagnostic-card"><h3>Pipeline diagnostics</h3><pre>{JSON.stringify(document.diagnostics, null, 2)}</pre></section><section className="diagnostic-card full-span"><h3>Canonical JSON</h3><pre>{JSON.stringify(document, null, 2)}</pre></section></div>; }
