# Optional. When set, the Python endpoint requires the same x-api-key header.
OCR_API_KEY=

# Public Vite variables are visible to browser users. Use only for a private preview,
# never as the sole protection for a public production endpoint.
VITE_OCR_API_KEY=

OCR_MAX_IMAGE_BYTES=3400000
OCR_MAX_TOTAL_BYTES=3600000
OCR_MAX_IMAGE_DIMENSION=2400
OCR_MAX_ITEMS=2500
